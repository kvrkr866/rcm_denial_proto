# ─────────────────────────────────────────────────────────────────────────────
# RCM Denial Management — Python Package Requirements
#
# Install ALL packages (core + web UI + dev tools):
#   pip install -r requirements.txt
#
# Or install via pyproject.toml (equivalent):
#   pip install -e ".[all]"
#
# System dependencies also needed (see below):
#   - tesseract-ocr (optional, for scanned PDF OCR fallback)
#   - poppler-utils (optional, needed only if tesseract is used)
# ─────────────────────────────────────────────────────────────────────────────

# ── AI Pipeline ──────────────────────────────────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-chroma>=0.1.0
chromadb>=0.5.0
openai>=1.40.0

# ── Data Models & Configuration ──────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# ── PDF Generation & OCR ─────────────────────────────────────────────────────
PyMuPDF>=1.24.0           # Primary: digital PDF text extraction (fast, accurate)
pytesseract>=0.3.10       # Fallback: scanned PDF OCR (needs tesseract-ocr system package)
pdf2image>=1.17.0         # Fallback: PDF to image for Tesseract (needs poppler-utils)
pypdf>=4.2.0              # PDF merging
reportlab>=4.2.0          # PDF generation (analysis reports, appeal letters)
fpdf2>=2.7.9              # Lightweight PDF creation
Pillow>=10.3.0            # Image processing for OCR

# ── Data Processing ──────────────────────────────────────────────────────────
pandas>=2.2.0

# ── Networking & Retry ───────────────────────────────────────────────────────
httpx>=0.27.0
tenacity>=8.3.0           # Retry with exponential backoff
prometheus_client>=0.20.0 # Push metrics to Prometheus Pushgateway

# ── CLI & Logging ────────────────────────────────────────────────────────────
click>=8.1.7
rich>=13.7.0
structlog>=24.1.0

# ── Web UI ───────────────────────────────────────────────────────────────────
nicegui>=2.0.0            # Browser-based operator console

# ── Dev / Testing ────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.4.0
mypy>=1.10.0
